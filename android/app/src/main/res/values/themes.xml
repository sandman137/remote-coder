<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the real theming (ui/Theme.kt); this is the launch/system
         theme applied before Compose takes over. -->
    <style name="Theme.Helm" parent="android:Theme.Material.NoActionBar" />
</resources>
