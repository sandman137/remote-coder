org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Rust cross-build inputs consumed by app/build.gradle.kts (:cargoNdkBuild).
# ANDROID_NDK_HOME must be set in the environment for the Rust build.
rcoder.rustAbis=arm64-v8a,x86_64
