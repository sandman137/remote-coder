//! Desktop harness: ratatui TUI (Phase 2) + headless CLI (Phase 1).
//! Phase 0: stub that proves the workspace wiring.

fn main() {
    println!("helm engine-cli (engine v{})", engine::version());
    println!("Phase 0 stub — headless CLI lands in Phase 1, TUI in Phase 2.");
}
