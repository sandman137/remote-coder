//! Desktop harness library: the TUI app state machine + grid re-rendering.
//! `main.rs` is a thin argv wrapper; tests drive these modules directly.

pub mod pairing_cmd;
pub mod render;
pub mod tui;
