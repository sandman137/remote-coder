//! `notifier notify --session agents --state waiting …` — invoked by agent
//! hooks (tier 1) and the tmux monitor daemon (tier 2). Implemented in Phase 7.

fn main() {
    eprintln!("helm-notifier: not yet implemented (Phase 7)");
    std::process::exit(1);
}
